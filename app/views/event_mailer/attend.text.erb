<%= "Hi {@user.fname}," if @users.length == 1 %>

This email confirms that you are attending <%= @event.display_name(@user) %>, <%= event_url(@event) %>


<%= render 'event', event: @event, user: @user -%>