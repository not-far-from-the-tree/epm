There have been changes to <%= Configurable.event.indefinitize %> you are attending, <%= @event.display_name(@user) %> <%= event_url(@event) %> . Here are the updated details:

<%= render 'event', event: @event, user: @user %>

If you are unable to attend the <%= Configurable.event %>, please visit the event page to cancel.
