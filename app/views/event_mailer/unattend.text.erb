Sorry, but you are no longer attending an event, <%= @event.display_name(@user) %> <%= event_url @event %>
<%= "\nThis was to take place on #{date @event.start} at #{time @event.start}." if @event.start %>

<%= 'This is because the maximum number of participants was lowered.' if @reason == 'max_changed' %>

You can see all the events you are attending on your profile page, <%= me_url %>