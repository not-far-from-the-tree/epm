<%= Configurable.event.indefinitize.capitalize %> is awaiting approval: <%= @event.display_name(@user) %>, <%= event_url(@event) %> . Here are the details:

<%= render 'event', event: @event, user: @user %>