<p><%= Configurable.event.indefinitize.capitalize %> is awaiting approval: <%= link_to @event.display_name(@user), event_url(@event) %>. Here are the details:<p>

<%= render 'event', event: @event, user: @user %>